engines:
  duplication:
    enabled: true
    config:
      languages:
      - ruby
  reek:
    enabled: true
  rubocop:
    enabled: true

exclude_paths:
- spec/**/*

ratings:
   paths:
   - "**.rb"
